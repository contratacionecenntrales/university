# Endpoint for the lead-capture webhook used by every LeadForm on the site.
# Point this at your GoHighLevel / n8n / Make inbound webhook URL — all three
# accept a plain JSON POST, so no vendor-specific payload shaping is required.
PUBLIC_WEBHOOK_URL=https://hooks.example.com/lead-capture
